<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
 
  <Target Name="Build">
    <MSBuild Projects="@(Content)"/>
  </Target>
  
  <Target Name="Clean">
    <MSBuild Projects="@(Content)" Targets="Clean"/>
  </Target>
  
  <Target Name="Rebuild">
    <MSBuild Projects="@(Content)" Targets="Rebuild"/>
  </Target>

</Project>

